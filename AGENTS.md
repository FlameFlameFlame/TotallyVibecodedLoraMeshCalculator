# AGENTS.md

## Repository Guardrails

- Never `git add` or commit anything under `projects/`.
- Treat `projects/` as local project data/output workspace only.
- Keep project experiments, generated outputs, and debug artifacts inside `projects/` so they remain untracked.
